<UserControl x:Class="Octarine_Core.Resource.UsersIntefeces.FriendAddList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Octarine_Core.Resource.UsersIntefeces"
             mc:Ignorable="d" 
             d:MaxHeight="450" d:MaxWidth="800">
    <Grid Height="450"  Width="500">

        <Rectangle Fill="#2C2C2C" RadiusX="15" StrokeThickness="0.4" RadiusY="15"  Stroke="{DynamicResource MainWhite}"  />
        <TextBox Margin="0,45,10,0" IsReadOnly="True" TextWrapping="Wrap" Text="Добавить друга в чат" FontSize="14" Height="28" TextAlignment="Center" Width="162" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        <Button x:Name="CloseBTN" Margin="0,10,10,0"  VerticalAlignment="Top" HorizontalAlignment="Right" Width="29" Height="30" Foreground="{x:Null}" BorderBrush="{DynamicResource MainWhite}" BorderThickness="0.2" Content="X" Background="{x:Null}" FontSize="24" Click="CloseBTN_Click"/>
        <ScrollViewer  Margin="5,80,5,72" Width="490">
            <StackPanel x:Name="MainStackPanel"/>
        </ScrollViewer>
        
        <Button x:Name="AddBTN"  Margin="0,0,10,10" HorizontalAlignment="Right" Width="107" Foreground="{x:Null}" BorderBrush="{DynamicResource MainWhite}" BorderThickness="0.2" Content="Добавить" Background="{x:Null}" FontSize="18" Height="30" VerticalAlignment="Bottom" Click="AddBTN_Click"/>
        <TextBox x:Name="ChatNameTb" Margin="123,0,184,14" Text="Имя чата" FontSize="14" Height="28" MaxLength="20" Background="{DynamicResource SecondGreen}" VerticalAlignment="Bottom"/>
        <TextBlock HorizontalAlignment="Left" Margin="20,0,0,16" TextWrapping="Wrap" Text="Имя чата:" FontSize="20" VerticalAlignment="Bottom"/>

    </Grid>
</UserControl>
